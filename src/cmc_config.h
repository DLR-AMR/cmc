#ifndef CMC_CONFIG_H
#define CMC_CONFIG_H

/* Include the autotools configuration macros */
#include "cmc_ac_config.h"
/* Include fallback macros (after the configuration macros) to ensure compilation if some librarires are not linked against CMC */
#include "utilities/cmc_fallbacks.h"


#endif /* CMC_CONFIG_H */